{
  "name": "@metuur/claude-live-monitor",
  "version": "0.2.4",
  "description": "Zero-dependency live monitor for Claude Code session transcripts (HTTP + SSE dashboard).",
  "type": "module",
  "bin": {
    "claude-live-monitor": "bin/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "CONTRACT.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "bun run collector.ts",
    "build": "bun build collector.ts --target node --outfile dist/collector.js && rm -rf dist/public && cp -r public dist/public && rm -rf dist/vendor && mkdir -p dist/vendor && cp src/vendor/o200k_base.tiktoken dist/vendor/",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "monitor",
    "tokens",
    "sse"
  ],
  "license": "MIT"
}
